```javascript
// pages/index.js

export default function Home() {
  return (
    <div>
      <h1>Welcome to my Next.js app!</h1>
    </div>
  );
}
```
// Check next.config.js for any experimental features and disable them temporarily to see if this resolves the build issue.
//Review any third-party libraries used for image optimization or data fetching and update them to the latest versions or consider alternative libraries if needed.